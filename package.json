{
    "name": "email-service",
    "version": "1.0.0",
    "description": "A resilient email sending service in TypeScript with retry, fallback, idempotency, rate limiting, and circuit breaker pattern.",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "test": "jest",
        "start": "ts-node src/index.ts"
    },
    "devDependencies": {
        "typescript": "^4.7.4",
        "ts-node": "^10.7.0",
        "jest": "^28.0.3",
        "ts-jest": "^28.0.1",
        "@types/jest": "^28.0.1"
    }
}

